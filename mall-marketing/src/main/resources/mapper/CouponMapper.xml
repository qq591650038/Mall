<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.mapper.CouponMapper">
    <update id="decrementRemain">UPDATE coupon SET remain_count = remain_count - 1, update_time = NOW() WHERE id = #{id}
        AND remain_count &gt; 0 AND status = 1 AND deleted = 0
    </update>
    <update id="incrementRemain">UPDATE coupon SET remain_count = remain_count + 1, update_time = NOW() WHERE id = #{id}
        AND deleted = 0
    </update>
</mapper>
