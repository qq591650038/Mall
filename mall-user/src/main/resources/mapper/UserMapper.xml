<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.mapper.UserMapper">
    <select id="selectAdminCursorPage" resultType="com.mall.entity.User">
        SELECT * FROM `user`
        <where>
            AND deleted = 0
            <if test="keyword != null and keyword != ''">
                AND (username LIKE CONCAT('%', #{keyword}, '%') OR nickname LIKE CONCAT('%', #{keyword}, '%')
                OR phone LIKE CONCAT('%', #{keyword}, '%') OR email LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null">AND status = #{status}</if>
            <if test="cursorTime != null">
                AND (create_time <![CDATA[ < ]]> #{cursorTime} OR (create_time = #{cursorTime} AND id <![CDATA[ < ]]>
                #{cursorId}))
            </if>
        </where>
        ORDER BY create_time DESC, id DESC
        LIMIT #{limit}
    </select>
</mapper>
